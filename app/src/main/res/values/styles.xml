<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@style/ButtonText</item>
    </style>
    <!--checked box color change-->
    <style name="color_checked_box" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!--default selected check box color-->
        <item name="colorPrimaryDark">@color/black</item>
        <!--unselected check box color-->
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
    </style>

    <style name="ButtonText" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/round_btn_recieved</item><!-- only way to change this is to change the src image -->

    </style>
    <style name="ShapeAppearanceOverlay.ButtonRounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">32dp</item>
    </style>
    <style name="AlertDialogTheme" parent="Theme.MaterialComponents.Light.Dialog.Alert">
        <item name="colorPrimary">@color/colorAccent</item>
    </style>

</resources>
